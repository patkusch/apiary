# public/

Remotion serves anything under this folder via `staticFile()`.

- `audio/bed.mp3` — Ambient synth bed (C0, generated offline via ffmpeg, CC0).
  Command preserved in `scripts/generate-audio.sh`.
